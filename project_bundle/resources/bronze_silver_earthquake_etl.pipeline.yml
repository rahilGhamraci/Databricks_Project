# The main pipeline for project_bundle

resources:
  pipelines:
    bronze_silver_earthquake_etl:
      name: Bronze_Silver_Prod
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob:
            include: ../src/DLT_pipelines/Bronze_Silver/transformations/**
      schema: 02_silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      run_as: 
        user_name : ghamracirahil@gmail.com
      root_path: ../src/DLT_pipelines/Bronze_Silver


